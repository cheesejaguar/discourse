{
  "name": "@hvnp/shared",
  "version": "1.0.0",
  "description": "Shared types and utilities for the Human-Verified News Platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.1.0",
    "typescript": "^5.3.2",
    "vitest": "^1.1.0"
  }
}
